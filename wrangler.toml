name = "vyrocni-zprava"
compatibility_date = "2026-01-01"
pages_build_output_dir = "public"

# Žádné placené služby. Potřeba je jen secret s Claude API klíčem:
#   Cloudflare Pages → Settings → Variables and Secrets → Secret: ANTHROPIC_API_KEY
# Lokálně: do souboru .dev.vars řádek  ANTHROPIC_API_KEY=sk-ant-...
# Nahrané soubory (fotky, logo, staré VZ) se ukládají v prohlížeči (IndexedDB), ne na server.
